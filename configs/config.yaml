# new config
defaults:
  - _self_
  - stage: vocab

_target_: cs336_basics.config.Config
prefix: debugs # enum [runs, debugs]
current_path: ${prefix}/${stage.cfg.name}/${now:%Y-%m-%d_%H-%M-%S}

hydra:
  run:
    dir: ${current_path}
