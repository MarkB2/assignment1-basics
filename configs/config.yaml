# Base config
defaults:
  - _self_
  - base_config
  - vocab: base
  - tokenizer: base

debug: debug/
stage: EVAL
# running directory
dir: ${debug}runs/${stage}_${now:%Y-%m-%d_%H-%M-%S}
# the parent manifest directory
parent: None
source: None

hydra:
  run:
    dir: ${dir}
