# new config
defaults:
  - stage: vocab
  - _self_

_target_: cs336_basics.runs.Runner
config:
  _target_: cs336_basics.config.Config
  stage: ${stage.config.name}
  prefix: debugs # enum [runs, debugs]
  current_path: ${config.prefix}/${config.stage}/${now:%Y-%m-%d_%H-%M-%S} # /${stage.cfg.name

hydra:
  run:
    dir: ${config.current_path}
